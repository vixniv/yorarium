// Shared game flags.
public static class GameState
{
    // True while the start menu covers the tank: the aquarium keeps living but taps and food drain wait.
    public static bool InMenu { get; set; } = true;
}
